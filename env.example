# API Keys for LLM (At least one required)
OPENAI_API_KEY=sk-your-openai-key-here
# GEMINI_API_KEY=your-gemini-key-here

# Workday Credentials (Required for Workday sites)
WORKDAY_EMAIL=your_email@example.com
WORKDAY_PASSWORD=your_secure_password
# Optional: Secondary password if the first one fails/needs reset logic
WORKDAY_PASSWORD_SECONDARY=your_secondary_password

# Optional: Google Search API (if using Serper/Google Jobs)
# SERPAPI_KEY=your-serpapi-key

# Optional: Model selection
# LLM_PROVIDER=openai  # or gemini

